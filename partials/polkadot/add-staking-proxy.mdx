import Link from "@docusaurus/Link";
import img1 from "@site/static/img/staking/workflows/v1/polkadot/Workflows::V1::Polkadot::AddProxyFlow_workflow.png";
import { APIReferenceNav } from "@site/src/components/APIReferenceNav";

## Add Staking Proxy

The Staking API's staking proxy flow for Polkadot will walk your application through the process of adding proxy rights to another account.

In the following steps we will illustrate how to add a proxy account.

### Flow Diagram

Below is a complete diagram for the Polkadot Add Proxy flow:

<img
  src={img1}
  alt="Polkadot Add Proxy Flow Diagram"
  className="inline-image"
/>

### Actions

<APIReferenceNav
  service="staking-api"
  network="polkadot"
  folders={undefined}
  path="docs/staking/02_networks/polkadot/index.mdx"
  methods={[
    {
      name: "Create New Add Staking Proxy Flow",
    },
    {
      name: "Submit Add Proxy Data",
    },
    {
      name: "Submit Signed Add Proxy Transaction for Broadcast",
    },
    {
      name: "Get Flow Status",
    },
  ]}
/>
